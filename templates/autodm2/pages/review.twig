{% extends 'app.twig' %}
{% block MAIN %}
{% include 'partials/crumbs.twig' %}
<hr class="m-0" />
<div class="container p-4 py-5">
    <h4 class="fw-bold mb-5">Отзыв {{resource.pagetitle}}</h4>
    <div class="row p-5">
      <div class='col-6'><span></span>
            <p>{{resource.longtitle}}</p>
        <div class="address__item"><span></span>
            {{resource.content |raw}}
        </div>
      </div>
      <div class="col-6">
            <img class='w-100' src="{{resource.image}}">

      </div>
      </div>
    </div>
  </div>


{% endblock %}