        <footer class="footer position-relative py-4">
        <a href="#top" class="totop position-absolute"></a>
        <div class="container d-flex">
            <div class="logo">
                <a href="/">
                    {% if baseSettings["logo"] %}
                        <img src="{{baseSettings["logo"]}}">
                    {% else %}
                        <div style="width: 200px;
                height: 48px;
                font-style: normal;
                font-weight: 400;
                font-size: 20px;
                line-height: 24px;
                display: flex;
                align-items: center;
                text-transform: uppercase;
                justify-content: center;
                color: #D12D1E;">{{ baseSettings.pagetitle }}
                        </div>
                    {% endif %}
                </a>
                <span class="align-middle pl-2 ml-2 text-light">{{baseSettings.salon_location}}</span>{#Вставил адрес здесь#}


            </div>
            <ul class="pt-2">
                <li class="d-inline-block"><a href="/avtomobili-s-probegom" class="text-light">Каталог</a></li>
                <li class="d-inline-block"><a href="/akcii2" class="text-light">Акции</a></li>
                <li class="d-inline-block"><a href="/kontakty" class="text-light">Контакты</a></li>
            </ul>
            {{  runSnippet('formatPhone', {number: baseSettings["salon-phone"], format: '+7 (xxx) xxx-xx-xx', link: '1', class: 'phone pt-2 text-light'}) | raw}} </a>
        </div>
    </footer>
    <div class="copyright-block text-center py-2">
        <div class="container">
            © {{baseSettings["pagetitle"]}}, 2023. Все права защищены
        </div> 
    </div>
    <div class="container"><p style="font-size: 8px;" class="pt-1">
</p>            <p style="font-size: 10px">{{baseSettings.footer |raw}}</p>
</div>
