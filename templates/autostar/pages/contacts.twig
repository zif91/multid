{% extends 'app.twig' %}

{% block MAIN %}
    {% include('partials/contacts.twig') %}

    <div class="s-address section-36">
        <div class="container">
            {{resource.content | raw}}
        </div>
    </div>
    <div class="s-address section-36">
        <div class="container">
            {{baseSettings.content | raw}}
        </div>
    </div>
{% endblock %}